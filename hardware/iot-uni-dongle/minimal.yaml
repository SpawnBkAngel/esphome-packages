# Minimal iot-uni-dongle config
substitutions:
  name: iot-uni-dongle

packages:
  # Common minimal config
  minimal: !include ../../common/minimal.yaml
  # HTTP server config
  web: !include ../../common/web.yaml
  # ESP-12E module minimal config
  esp8266: !include ../esp01s/esp8266.yaml
